interface MainColor {
    /**
     *
     * 接口地址：
     * https://public.choiceform.com/fake-request-exploits/distatest/main/color/age_verify
     *
     * @param value 
     */
    age_verify(value: string): { result: string, message: string };
}
